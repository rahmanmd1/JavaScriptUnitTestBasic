//====  Upper case

function toUpper(text) {

  if (text) {
    return text.toUpperCase();
  }
  return NaN;
}

//====  Upper case
function toLower(text) {
  if (text === undefined) {
    return text.toLowerCase();
  }
  return NaN;
}
